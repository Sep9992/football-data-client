name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep database alive
        run: |
          psql '${{ secrets.postgresql://postgres:Konikova1112@db.wzficawmmbqaftsosznm.supabase.co:5432/postgres }}' -c "SELECT 1"
          
